# Neon Postgres (Vercel Marketplace) or a Neon connection string
# https://neon.com/docs/guides/vercel-postgres-transition-guide
DATABASE_URL=

# Legacy Vercel Postgres variable names still work if already set
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# `openssl rand -base64 32`
AUTH_SECRET=
AUTH_URL=http://localhost:3000/api/auth
